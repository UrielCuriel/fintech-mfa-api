repos:
  - repo: local
    hooks:
      - id: run-pytest
        name: Run pytest
        entry: pytest tests/
        language: system
        types: [python]
        stages: [pre-commit, pre-push]

